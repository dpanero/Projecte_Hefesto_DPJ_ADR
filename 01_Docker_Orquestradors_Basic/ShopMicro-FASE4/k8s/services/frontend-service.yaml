apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: shopmicro
  labels:
    app: frontend
spec:
  type: LoadBalancer
  selector:
    app: frontend
  ports:
  - port: 8080
    targetPort: 80
