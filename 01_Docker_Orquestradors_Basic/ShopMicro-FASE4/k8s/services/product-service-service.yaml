apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: shopmicro
  labels:
    app: product-service
spec:
  selector:
    app: product-service
  ports:
  - port: 5000
    targetPort: 5000
